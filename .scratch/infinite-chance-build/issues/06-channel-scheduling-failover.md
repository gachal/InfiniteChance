# 06 — 多渠道调度:选择/换道重试/熔断 + 模型列表

**What to build:** 同一公开模型挂多渠道时按优先级/权重选择;上游临时故障在请求尚未被计费前自动换道重试;渠道连续失败达阈值即熔断禁用,恢复后半开探测自动放回;`/v1/models` 返回全部启用渠道映射出的公开模型。

**Blocked by:** 04

**Status:** ready-for-agent

- [ ] 主渠道故障自动落到备用渠道,客户端无感(fake 上游模拟)
- [ ] 连续失败达阈值后渠道不再被选中;恢复后半开探测自动恢复
- [ ] `/v1/models` 列出所有可用公开模型,无重复
