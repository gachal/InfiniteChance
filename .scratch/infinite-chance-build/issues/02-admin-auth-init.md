# 02 — 管理端鉴权与管理员初始化

**What to build:** 全新库首次访问出现初始化引导,创建唯一管理员账号(密码哈希入库);登录端点校验后签发 JWT;网关侧的管理 API 用它保护,canvas/server 共享同一 JWT 校验(签名密钥经 env 共享);admin-web 提供登录页,未登录访问受保护页面跳转登录。

**Blocked by:** 01

**Status:** done

- [x] 全新库首次访问出现初始化引导,设置后账号密码可登录
- [x] 登录签发的 JWT 可访问受保护端点;无 token 或过期返回标准 401
- [x] canvas/server 能校验网关签发的 JWT(同一账号体系)
- [x] 密码仅以哈希存储;初始化完成后引导不再出现
